{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Ryan's todo app",
  "main": "index.js",
  "repository": "https://github.com/ryanzg123/todo-app.git",
  "author": "Ryan Gan <49723210+ryanzg123@users.noreply.github.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "setup": "rm -f database.db && sqlite3 database.db < setup.sql",
    "build": "rm -rf out && mkdir out && tsc",
    "start": "cd out && node server.js",
    "watch": "yarn run build && tsc-watch --onSuccess \"yarn run start\"",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --runInBand --rootDir=src"
  },
  "type": "module",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "typescript": "^5.0.4"
  }
}
